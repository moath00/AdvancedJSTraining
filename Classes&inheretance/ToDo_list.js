class Task {

    // state getter and setter
    get state () {
        return this.state;
    }
    set state (state) {
        state;
    }
    // due date getter and setter
    get dueDate () {
        return this.dueDate;
    }
    set dueDate (dueDate) {
        dueDate;
    }

    // priority getter and setter
    get priority () {
        return this.priority;
    }
    set priority (priority) {
        priority;
    }
}

const main = () => {
    const tasksList = [];
    const test = new Task();
    tasksList.push(test);
    let state = process.stdin.read();
    test.prototype.state;
}

main();